PORT=3008

MONGODB_URI=mongodb://localhost:27017/trato_notifications

# Internal API key for service-to-service communication (POST /notifications)
INTERNAL_API_KEY=trato-internal-api-key-dev-2026

# Auth service URL (for JWT validation on user-facing endpoints)
AUTH_SERVICE_URL=http://localhost:3001

# Notify secret (permite que microservicios llamen directo con x-notify-secret)
NOTIFY_SECRET=trato-notify-secret-dev-2026
